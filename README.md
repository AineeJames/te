# te (text engine)

A simple game enginine that mimics a terminal.

# TODO

- More lua api functions (te.graphics.print("some text", x, y))
- Sound support
- exit codes
- for te.graphics.setCell(): accept lua number insteadof int and floor for grid
